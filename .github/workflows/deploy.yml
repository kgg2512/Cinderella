name: Deploy to Vercel
on:
  push:
    branches: [master]
    paths: [cinderella/**]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: npm ci
        working-directory: cinderella
      - run: npx vercel --prod --token ${{ secrets.VERCEL_TOKEN }} --cwd cinderella
        env:
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
